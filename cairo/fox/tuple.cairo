// Destructure the `cat` tuple to call print on each member. It is like in Rust.
// I AM NOT DONE

use debug::PrintTrait;

fn main() {
    let king = ('Charles III', 74);
    let // your pattern here = king;
    name.print();
    age.print();
}
