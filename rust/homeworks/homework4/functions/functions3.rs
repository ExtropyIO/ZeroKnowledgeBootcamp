// functions3.rs
// Make me compile! Execute `zustlings hint functions3` for hints :)

// I AM NOT DONE

fn main() {
    call_this();
}

fn call_this(num: u32) {
    for i in 0..num {
        println!("Loop now {}", i + 1);
    }
}
